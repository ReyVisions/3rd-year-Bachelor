#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include <ctype.h> 
#include "Arbre.c"


int main(){
    UI();
    /* TESTS SANS UI
    Arbre *arbre= (Arbre*) malloc(sizeof(Arbre));

    printf("On initialise l'arbre");
    fflush(stdout);
    initArbre(arbre);

    printf("\nOn ajoute les valeurs");
    fflush(stdout);

    remplirArbreParOrdre(0,intEnArbo(0),arbre);
    remplirArbreParOrdre(1,intEnArbo(1),arbre);
    remplirArbreParOrdre(2,intEnArbo(2),arbre);
    
    afficherArbre(arbre->racine);
    effacerNoeud(intEnArbo(2),arbre);

    afficherArbre(arbre->racine);
    free(arbre);*/
}